{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if [ -n \"$(git status --porcelain 2>/dev/null)\" ]; then git add -A && git commit -m \"autocommit: $(date +%Y-%m-%d_%H:%M:%S)\"; fi'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
