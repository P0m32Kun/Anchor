# 用户部署 — 复制为 .env 后由 install.sh 写入或自行填写
ANCHOR_API_TOKEN=change-me
ANCHOR_MODE=server_worker
# 可选：覆盖 ACR 镜像前缀（默认见 compose 内 ANCHOR_REGISTRY）
# ANCHOR_REGISTRY=crpi-wthv8jhah5ufmzlr.cn-hangzhou.personal.cr.aliyuncs.com/p0m32kun

# 功能测试 / 被动搜索 API 密钥见 functional-test.env.example（复制为 functional-test.env）
